# 反向代理 / TLS 终止由宿主或集群侧（nginx / Caddy / 云 LB / k8s ingress）负责。
# 本文件当前不引入任何服务，仅作为 prod 环境的覆盖钩子；如需新增 prod 专属
# 服务（例如多实例 aim-gateway 副本、prod-only sidecar），请把 YAML 片段补到下方
# services: 块下，并继续以 `expose:` 暴露端口，避免把内部端口直接映射到宿主机。

services: {}
